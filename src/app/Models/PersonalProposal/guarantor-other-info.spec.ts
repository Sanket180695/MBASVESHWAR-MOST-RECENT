import { GuarantorOtherInfo } from './guarantor-other-info';

describe('GuarantorOtherInfo', () => {
  it('should create an instance', () => {
    expect(new GuarantorOtherInfo()).toBeTruthy();
  });
});
