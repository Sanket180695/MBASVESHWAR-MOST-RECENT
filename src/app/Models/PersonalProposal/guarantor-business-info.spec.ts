import { GuarantorBusinessInfo } from './guarantor-business-info';

describe('GuarantorBusinessInfo', () => {
  it('should create an instance', () => {
    expect(new GuarantorBusinessInfo()).toBeTruthy();
  });
});
