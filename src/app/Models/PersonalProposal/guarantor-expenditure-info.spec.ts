import { GuarantorExpenditureInfo } from './guarantor-expenditure-info';

describe('GuarantorExpenditureInfo', () => {
  it('should create an instance', () => {
    expect(new GuarantorExpenditureInfo()).toBeTruthy();
  });
});
